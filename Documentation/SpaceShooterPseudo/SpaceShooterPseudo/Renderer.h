#pragma once

struct Renderer
{

	void displayGalaxyMap(void)
	{
		/*The galaxy map gives the same information as the long range scan,
		but for the entire galaxy.  If a quadrant has not been visited 
		or scanned, the data in the galaxy map will be blank. 
		The galaxy map reflects that last known state of each quadrant.
		The galaxy map is not affected by critical systems damage.*/
	}
};

